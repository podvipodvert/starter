/**
 * Created by andrew on 11.04.17.
 */

const initialState = {
    email:'',
    password:''
};

function main(state: State = initialState, action: Action) {
    switch (action.type) {

        default:
            return state
    }
}

module.exports = main;